import React from 'react';

import './Header.css';

export const Header = () => (
    <div className="header">
        Notes App
    </div>
);

export const Footer = () => (
    <div className="footer">
        developed in react
    </div>
);
